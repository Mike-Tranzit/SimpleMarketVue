import Vue from 'vue'
import Vuex from 'vuex'
import goodsStore from './goods.store';

Vue.use(Vuex)

export default new Vuex.Store({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    goodsStore
  }
})
